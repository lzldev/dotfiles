# add 'source ~/.zshaliases' to ~/.zshrc

alias emulator="emulator -avd emulator01"

alias lz=lazygit
alias lzd=lazydocker
alias pn=pnpm
alias ctl=systemctl
alias src="source ~/.zshrc"

# replaces cat with batcat
alias cat=bat
alias ocat=/bin/cat

